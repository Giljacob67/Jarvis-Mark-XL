# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Configurações sensíveis
config/api_keys.json

# Logs
*.log
logs/

# Modelos locais
models/
*.bin
*.gguf

# Runtime data
memory/*.json
memory/*.db
memory/*.db-journal
memory/clipboard_history.json

# Certificados TLS do dashboard remoto (chave privada — NUNCA commitar)
config/certs/

# Perfil pessoal do usuário (dados sensíveis — nunca commitar)
memory/user_profile.md

# OAuth Google (client secret + token do usuário — nunca commitar)
config/google_credentials.json
config/google_token.json
